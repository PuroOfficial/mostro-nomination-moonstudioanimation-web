/** @type {import('next').NextConfig} */
const nextConfig = {
  // REMOVE THESE LINES:
  // basePath: '/mostro-nomination',
  // assetPrefix: '/mostro-nomination/',
  trailingSlash: true,
};

module.exports = nextConfig;